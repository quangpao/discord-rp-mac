## What does this change?

<!-- One or two sentences. Link the issue if there is one. -->

## How was it verified?

<!-- Real evidence, please: the commands you ran and what they printed.
     e.g. `swift test` (87/87), `--presets` output, the rendered PNG from `--render-editor`. -->

## Checklist

- [ ] `swift test` passes
- [ ] Commit message is one line, English, imperative
- [ ] No API key, no personal Discord Application ID, no personal Giphy URL, no personal data in
      code, tests, docs or screenshots
- [ ] If the UI changed: verified with `--render-editor` (or a screenshot) rather than by assumption
- [ ] If a Discord rule changed: the rule is stated in a comment or test, not just in the code path
