CustomRP — Editor Window Redesign · SwiftUI Handoff Spec
Native macOS · SwiftUI handoff
CustomRP Editor Window — Redesign Spec
Dark
A ground-up rebuild of the 480 × 620 grouped Form. Two-tier row system, one fixed label column, one section-header treatment, and no control ever sharing a line with a full-length URL. Every dimension below maps to SwiftUI/AppKit primitives — no custom drawing, no Canvas, no hand-rolled layout.
1:1 mocks · light + dark
480 × 620 minimum
560 × 720 recommended
22 pt control height
13 pt body · 11 pt secondary
01Overview & recommendation
The window keeps its content and its grouped-Form character. What changes is the grid underneath it: a fixed label column, a second stacked tier for long fields, and a single consistent home for helper text and actions.
Recommend 560 × 720 as the default. At 480 pt the card interior is 428 pt; after the 132 pt label column a standard field gets 288 pt — enough for short values but not for the two URL rows, which is why the old window truncated them. At 560 pt every inline field gets 368 pt and the squeeze disappears entirely. Keep 480 × 600 as the minimum so the window still behaves on a 13" MacBook in Split View.
Window
560 × 720 default
480 × 600 minimum
Grid
132 pt label column + fluid control column
Row tiers
Inline (label left) and stacked (label above, full-width field)
Type
13 pt body · 11 pt secondary · 11 pt semibold headers
Controls
22 pt height · 6 pt radius · system blue accent
Actions
One primary (footer Apply); everything else bordered or borderless
02Window mocks, 1:1
Both sizes at true scale, light and dark. The content area scrolls and the footer stays pinned — exactly how the real window behaves. Compare the top of each window: every section now opens with a header on the same baseline, Connection included.
480 × 620Minimum supported size — the current default.
560 × 720Recommended default. Every inline field gains 80 pt of width.
03Detail crops
The regions that were broken, isolated at 1:1. Each appears in both appearances so field contrast, disabled styling and error colour can be checked directly.
04What changed and why
One row per problem from the review. “Metric” is the number that proves the fix — it is what the implementer should hold the build to.
#
Old layout
New layout
Metric that makes it work
1
“Time” and “Images” headers sat outside their cards and the first card had none, so the top edge read as unfinished.
Every section opens with the same 11 pt semibold header, 6 pt above its card. The Connection card is now headed “Connection”.
1 header style across 5 sections; header→card gap fixed at 6 pt; 20 pt between sections.
2
Explanations appeared inside cards, above controls, and wedged between a picker and a date picker.
All helper text is 11 pt secondary and lives in exactly two places: the card’s lead note (whole-card scope) or a footer row directly under the field it describes.
1 typographic treatment (11 pt, secondaryLabel); 4 pt gap to the field above.
3
“Load asset names” sat inside the data card, mixed in with editable fields.
Row-level actions (Upload…, Assets, Add button) sit in a dedicated trailing action row inside their group; card-wide actions sit in the action row under the card’s fields.
No action ever shares a row with a data field; 8 pt between grouped buttons.
4
“Party current” / “Party max” were two loose fields side by side with no column or separation.
One inline row: label “Party”, then two 46 pt numeric fields separated by a slash and closed by the 11 pt caption “current / max”.
Both fields share one baseline and one left edge; 6 pt between them.
5
No fixed label column — label widths varied, so values did not line up and short labels pushed values toward the right edge.
A single 132 pt label column, left-aligned, with an 8 pt gap; the control column takes the remainder.
132 pt fits the longest label (“Shown as (Discord app name)”) with room to spare; zero truncation.
6
Long label + full URL + “Upload…” forced onto one 480 pt line, so the URL was truncated and nearly uneditable.
URL rows use the stacked tier: label above, full-width field, and the actions moved to their own row underneath.
Field spans the full card interior — 428 pt at 480, 508 pt at 560; never below 288 pt.
7
The date picker rendered 9/ 1/2002, 17:25 — cramped, with odd spacing around the stepper.
DatePicker(.field) goes full width in the stacked tier: calendar glyph, date, flexible spacer, time pinned right behind a hairline.
One 22 pt control spanning 428/508 pt; time always right-aligned, 9 pt inset.
8
Link and optional text fields had no border or background, so an empty field was indistinguishable from a static label.
Every editable field uses the same 22 pt bordered surface and a tertiary placeholder; read-only values render as plain text instead.
1 field surface for all inputs; 1 pt border at 17% black/white; 6 pt radius.
9
The 480 pt window was too narrow for the three-column rows it attempted.
Three-column rows are gone. The layout allows at most two columns (label + control), and URL rows collapse to one.
Max 2 columns per row; ≥ 288 pt for every inline control at the minimum size.
05Handoff — control by control
Follow this literally. Spacing scale is 4 / 6 / 8 / 10 / 16 / 20. Section header = 11 pt semibold, secondaryLabel, 6 pt above the card. Inline row = HStack(spacing: 8) with the label at .frame(width: 132, alignment: .leading) and min-height 28 pt. Stacked row = one Section row holding VStack(alignment: .leading, spacing: 4) per field, 10 pt between fields.
Control
Exact label
SwiftUI element
Layout container
Width behaviour
Notes
Application ID
Discord Application ID
TextField
Form › Section › inline row
Fills control column
.textFieldStyle(.roundedBorder); validate on commit; error text under the field.
Pipe index
Pipe index
TextField + Stepper
One row, joined HStack(spacing: 0)
44 pt value + 19 pt chevrons
Clamp 0...9; shared 6 pt radius, hairline between the two halves.
Connection action
Apply
Button
Action row aligned to the control column
Hugs content
Bordered, not prominent — the footer Apply is the only primary button in the window.
Connection status
Connected as quangpao
HStack { Circle; Text }
Section footer row
Fills, wraps
11 pt; dot 7 pt. Other states: Discord not running (amber), Invalid Application ID (red).
Preset name
Preset name (this app only)
TextField
Inline row
Fills
App-local only; never sent to Discord.
Shown as
Shown as (Discord app name)
TextField
Inline row
Fills
Longest label — it sets the 132 pt column width.
Type
Type
Picker (.menu)
Inline row
.frame(minWidth: 150, alignment: .leading)
Playing · Streaming · Listening to · Watching · Competing in. Selecting Competing disables the whole Time card.
Show as
Show as
Picker (.menu)
Inline row
minWidth 150
Name · Details · State.
Details
Details
TextField
Inline row
Fills
Validate ≥ 2 characters; error inline.
Details link
Details link (optional)
TextField
Stacked group (single row)
Full card width
Empty state keeps the bordered surface plus placeholder.
State
State
TextField
Inline row
Fills
—
State link
State link (optional)
TextField
Stacked group (single row)
Full card width
—
Party
Party
two TextField
Inline row › HStack(spacing: 6)
46 pt each
Trailing 11 pt caption current / max; numeric format, tabular figures.
Mode
Mode
Picker (.menu)
Inline row
minWidth 196
Off (no timer) · Since Discord connect · Since app launch · Since last update · Local time · Custom.
Mode explanation
—
Text
Section footer, immediately after the Mode row
Full width
11 pt secondary; swaps with the selected mode. Live, not static.
Start
Start
DatePicker (.field)
Stacked group
Full card width
Shown only when Mode = Custom.
Show end
Show end
Toggle
Inline row, control trailing
Hugs
Reveals and activates End. Custom only.
End
End
DatePicker (.field)
Stacked group
Full card width
Disabled until Show end is on.
Time disabled note
—
Text
Card footer
Full width
The “Competing” type cannot show timestamps. Stays at full contrast while the controls drop to 42%.
Images note
—
Text
Card lead row
Full width
A key is either an asset name you uploaded to your Discord app, or an https URL.
Large slot · key
key or URL
TextField
Stacked group under the “Large” subheader
Full card width
Never truncated at either window size.
Large slot · text
text (optional)
TextField
Same stacked group
Full card width
10 pt below the key field.
Large slot · link
link (optional)
TextField
Same stacked group
Full card width
—
Large slot · actions
Upload… · Assets
Button · Menu
Action row under the group
Hugs, left-aligned to the control column
Upload pushes to Giphy and writes the returned URL into key; Assets lists uploaded asset names.
Small slot
key or URL · text (optional) · link (optional) · Upload… · Assets
Same five controls
Repeats the Large group under the “Small” subheader
Full card width
Identical structure; only the subheader text changes.
Giphy privacy
Private on Giphy
Toggle
Inline row
Trailing
One shared setting for both slots.
Upload status
Uploaded → … (Large)
HStack { Image; Text }
Section footer row
Full width, wraps
Green check on success; red warning plus foregroundStyle(.red) on failure.
Button label
Button N label
TextField
Stacked group under the “Button N” subheader
Full card width
Byte count validated against Discord’s 32-byte limit.
Button URL
Button N URL
TextField
Same stacked group
Full card width
—
Remove
Remove
Button (.borderless, destructive)
Subheader row, trailing
Hugs
One per button row.
Add button
Add button
Button
Action row after the last button group
Hugs
.disabled(buttons.count >= 2); the disabled state must stay visibly disabled.
Footer actions
Apply · Save · Save as New
Button ×3
Window footer HStack(spacing: 10)
Trailing
Apply = .borderedProminent (⌘R); Save = .bordered (⌘S); Save as New = .borderless. Default button rightmost, per macOS.
Validation text
e.g. Details needs at least 2 characters.
Text
Footer, above the buttons
Full width, wraps
11 pt red; one line per failure; never truncate the message.
06Layout spec
The numbers to build against. All values in points.
Window & page
Default size
560 × 720 · .windowResizability(.contentSize)
Minimum size
480 × 600
Window radius
10 pt (system)
Title bar
52 pt, title centered, 13 pt semibold
Outer padding
16 pt horizontal · 16 pt top · 4 pt bottom
Footer
Pinned · 10/16 pt padding · 0.5 pt top hairline
Spacing & grid
Section spacing
20 pt
Header → card
6 pt
Card radius
10 pt · controlBackgroundColor
Row padding
4 pt vertical · 10 pt horizontal · min-height 28 pt
Label column
132 pt fixed, 8 pt gap to control
Separators
0.5 pt, inset 10 pt both sides
Stacked field gap
4 pt label→field · 10 pt field→field
Field width floor
288 pt (inline, at the 480 pt window)
Controls
Control height
22 pt · 6 pt radius
Field surface
Bordered · 1 pt at 17% black/white
Picker
Picker(.menu) · min 150 pt (Mode 196 pt)
Stepper
Joined cluster: 44 pt value + 19 pt chevrons
Toggle
28 × 16 pt · 13 pt knob · accent when on
Checkbox
14 × 14 pt · 3.5 pt radius
Button
22 pt · 11 pt side padding · 6 pt radius
Party fields
46 pt each · 6 pt gap · slash separator
Type scale
Body / field / label / button
13 pt regular
Window title
13 pt semibold
Section header
11 pt semibold · secondaryLabel
Helper / status / validation
11 pt regular · line-height 1.42
Keyboard hint
10 pt · monospaced
Numeric
Tabular figures on numbers, dates, byte counts
States
Focus ring
3 pt accent at 30% + 1 pt accent border · on every focusable control
Disabled
42% opacity · secondary label · no shadow · not focusable
Error
Red border + 3 pt red at 22% ring · 11 pt red message in the section footer
Accent
systemBlue #007AFF light · #0A84FF dark
Error red
Fill #FF3B30 / #FF453A · text #C0261A / #FF453A
Hover
Bordered buttons darken one step; text colour never changes alone
07Rules
Non-negotiable behaviours the build is checked against.
Full keyboard access.Tab order follows visual order, top to bottom, footer buttons last. Every control shows a visible :focus-visible ring.
Empty optional fields look editable.Same 22 pt bordered surface as a filled field, with a tertiary placeholder — never a borderless line that reads as a static label.
URLs are fully readable.One full-width field in the stacked tier. Trailing actions move to their own row and never squeeze the field. No ellipsis on a key or link.
Labels never truncate.The 132 pt column fits the longest label; stacked labels get the full card width. If a label is added later, widen the column — do not clip it.
Disabled is visibly disabled.42% opacity plus a non-accent fill. The card keeps its height and layout so nothing jumps when a control enables.
“Competing in” looks intentional.The Time card stays in place with its controls dimmed and the note The “Competing” type cannot show timestamps. in the footer. It never collapses or disappears.
One primary action per view.Only the footer Apply is prominent. The Connection Apply stays bordered and “Save as New” is borderless.
Errors state the fix.Messages name the field and the limit, e.g. Button 1 label is 40 bytes; Discord allows 32. Never truncate a validation message.
CustomRP editor window — SwiftUI handoff spec. Each mock is a static reproduction of the target layout at native scale; the scroll region and pinned footer inside every window are live.
